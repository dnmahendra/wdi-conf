var express = require('express');
var router = express.Router();

router.get('/', function(req, res) {
  // var queryResults = require('./../mongoose-query');
  // console.log(queryResults);
});

module.exports = router;
